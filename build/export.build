$out_root/
{
  include src/
}

if ($($out_root/src/ cxx.target.class) == 'windows')
  assert ($import.target != libs{benchmark}) 'Google Benchmark only supports static library on windows.'
  
export $out_root/src/$import.target
